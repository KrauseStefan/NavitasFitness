application: navitas-fitness
version: 1
runtime: go
api_version: go1

basic_scaling:
  max_instances: 1
  idle_timeout: 5m

handlers:
- url: /static
  static_dir: static

- url: /.*
  script: _go_app
