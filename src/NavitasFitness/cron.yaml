cron:
- description: "Daily subscription about to expire user notification task"
  # url: /rest/subscriptionExpiration/send
  url: /rest/subscriptionExpiration/dryRun
  schedule: every 24 hours
  timezone: CET