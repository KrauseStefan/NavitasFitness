{
  "scripts": {
    "build": "tsc",
    "install": "webdriver-manager update",
    "test": "npm-run-all test:*",
    "test-serial": "yarn clean && yarn build && protractor",
    "test:E2E": "yarn clean && yarn build && protractor --parallel",
    "test:tslint": "tslint --project .",
    "clean": "rimraf PageObjects/*.js && rimraf specs/*.js && rimraf *.js && rimraf reports"
  },
  "dependencies": {
    "@google-cloud/datastore": "6.3.0",
    "@types/jasmine": "2.5.47",
    "@types/jasminewd2": "2.0.2",
    "@types/node": "14",
    "command-line-args": "4.0.3",
    "npm-run-all": "3.1.2",
    "protractor": "7",
    "protractor-beautiful-reporter": "1.3.6",
    "tslint": "5.11.0",
    "typescript": "4"
  },
  "devDependencies": {}
}
